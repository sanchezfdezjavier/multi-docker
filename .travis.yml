sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t sanchezfdezjavier/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true sanchezfdezjavier/react-test npm test

after_success:
  - docker build -t sanchezfdezjavier/multi-client ./client
  - docker build -t sanchezfdezjavier/multi-nginx ./nginx
  - docker build -t sanchezfdezjavier/multi-server ./server
  - docker build -t sanchezfdezjavier/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push sanchezfdezjavier/multi-client
  - docker push sanchezfdezjavier/multi-nginx
  - docker push sanchezfdezjavier/multi-server
  - docker push sanchezfdezjavier/multi-worker

  # Log into the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push sanchezfdezjavier/multi-client
  - docker push sanchezfdezjavier/multi-nginx
  - docker push sanchezfdezjavier/multi-server
  - docker push sanchezfdezjavier/multi-worker
